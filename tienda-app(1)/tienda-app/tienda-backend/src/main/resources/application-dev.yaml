spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tienda_dev
    username: root
    password: mysql

  jpa:
    show-sql: false

    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        use_sql_comments: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.springframework.orm.jpa: DEBUG
    org.springframework.transaction: DEBUG