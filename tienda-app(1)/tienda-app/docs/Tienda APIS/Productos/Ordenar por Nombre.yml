info:
  name: Ordenar por Nombre
  type: http
  seq: 15

http:
  method: get
  url: http://localhost:8080/api/v1/productos/consulta?orden=nombre&direccion=asc
  params:
    - name: orden
      value: nombre
      type: query
    - name: direccion
      value: asc
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
