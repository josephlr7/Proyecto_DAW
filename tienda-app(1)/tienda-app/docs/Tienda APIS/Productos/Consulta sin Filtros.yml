info:
  name: Consulta sin Filtros
  type: http
  seq: 8

http:
  method: get
  url: http://localhost:8080/api/v1/productos/consulta
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
