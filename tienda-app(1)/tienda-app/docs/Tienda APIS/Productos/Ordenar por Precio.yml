info:
  name: Ordenar por Precio
  type: http
  seq: 16

http:
  method: get
  url: http://localhost:8080/api/v1/productos/consulta?orden=precio&direccion=asc
  params:
    - name: orden
      value: precio
      type: query
    - name: direccion
      value: asc
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
