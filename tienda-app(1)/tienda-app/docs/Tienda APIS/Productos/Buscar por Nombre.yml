info:
  name: Buscar por Nombre
  type: http
  seq: 9

http:
  method: get
  url: http://localhost:8080/api/v1/productos/consulta?nombre=ipad
  params:
    - name: nombre
      value: ipad
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
