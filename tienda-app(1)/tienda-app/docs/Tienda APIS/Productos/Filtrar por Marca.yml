info:
  name: Filtrar por Marca
  type: http
  seq: 10

http:
  method: get
  url: http://localhost:8080/api/v1/productos/consulta?marca=Apple
  params:
    - name: marca
      value: Apple
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
