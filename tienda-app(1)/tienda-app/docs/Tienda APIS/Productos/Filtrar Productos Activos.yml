info:
  name: Filtrar Productos Activos
  type: http
  seq: 12

http:
  method: get
  url: http://localhost:8080/api/v1/productos/consulta?activo=true
  params:
    - name: activo
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
