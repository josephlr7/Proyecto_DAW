info:
  name: Ordenar by Fecha Creacion
  type: http
  seq: 12

http:
  method: get
  url: http://localhost:8080/api/v1/clientes/consulta?sort=fechaCreacion,desc
  params:
    - name: sort
      value: fechaCreacion,desc
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
