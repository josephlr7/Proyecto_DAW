info:
  name: Obtener Cliente by ID
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/v1/clientes/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
