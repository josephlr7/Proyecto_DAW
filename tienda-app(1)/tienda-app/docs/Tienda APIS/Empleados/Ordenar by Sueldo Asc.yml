info:
  name: Ordenar by Sueldo Asc
  type: http
  seq: 11

http:
  method: get
  url: http://localhost:8080/api/v1/empleados/consulta?sort=sueldo,asc
  params:
    - name: sort
      value: sueldo,asc
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
