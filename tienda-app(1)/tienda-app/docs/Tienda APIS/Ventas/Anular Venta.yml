info:
  name: Anular Venta
  type: http
  seq: 5

http:
  method: patch
  url: http://localhost:8080/api/v1/ventas/1/anular
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
